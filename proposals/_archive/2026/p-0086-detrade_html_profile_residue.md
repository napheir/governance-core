---
id: P-0086
agent: core
status: implemented
created: 2026-05-30
approved_at: 2026-05-30
implemented_in: dadc8eb
implemented_at: 2026-05-30
owner: core
---

# Proposal P-0086: De-trade residual trade-domain examples in knowledge-html-profile (P-0078 cluster cleanup)

## Trigger

Follow-up to P-0085 curation, on explicit user direction ("也一并处理"). While
de-trade-ifying the promoted §2.5, a grep gate surfaced **pre-existing**
trade-domain residue in `knowledge-html-profile.md` that predates this session
— shipped upstream during the P-0078 HTML-profile cluster promotion without
full de-trade-ification. governance-core's profile is **generic infra shipped
to every consumer** (Art.11.1), so another consumer's domain vocabulary in the
illustrative examples is a leak: it confuses a generic reader and contradicts
the de-trade discipline (`/curate-candidate` step 3).

Proposal governance applies: package-source knowledge change shipped via
`upgrade` (Art.11 package-source change).

## Scope

Edit **package source only**
(`governance_core/knowledge_governance/knowledge-html-profile.md`); genericize
the consumer-domain illustrations to governance-core's own domain. **Mechanism
/ contracts / structural rules unchanged — only example wording.** Profile
v1.1.0 → **v1.1.1** (Patch per §9: 澄清, 无契约变更).

Residue to clean (full inventory from grep gate):
1. **§2.2.1 caption** (`例（trade pin 自己的 pipeline_current.html）`) → neutral
   "pin a current-state doc to the dashboard briefing" caption.
2. **§3.3.1 attribution** (`trade 2026-05-26 首次 HTML pilot`) → drop the agent
   name, keep the generic incident date + lesson.
3. **§3.3.1 Mermaid pitfall examples** (`Stage 0<br/>signal_reader<br/>dedup`,
   `ART[artifacts/trade/{ts}/]`) → gc-domain pipeline labels + neutral artifacts
   path. The syntax lesson (raw HTML / bare `{` need quotes) is preserved verbatim.
4. **§4.1 autogen worked example** (`data-autogen-id="s50-tier-metrics"`,
   `data-source="artifacts/strangle50/.../metrics.json"`, the Tier/Precision/
   Reach table) → a gc-domain autogen example (governance audit summary).
5. **§7 pilot row** (`knowledge/models/s50_current.html`, owner=rules) → neutral
   current-state pilot description (owned by the consumer's domain agent).
6. **Status footer** (`Phase 5 (rules agent) pilot s50_current.html`) → neutral
   "consumer current-state pilot".
7. Add a **§9 version-history** entry for v1.1.1.

## Non-Goals

- **No mechanism / contract / structural-rule change** — §2.1–§2.4 / §3 syntax
  rules / §4 autogen attribute contract all stay; only example *content* changes.
- **No `audit_html_profile.py` change** and **no audit-tool bump** (examples are
  not machine-audited).
- Keep legitimate gc content: §7 `harness_defense.html` (owner=core) row, and
  the §9 v1.1.0 "trade-agent" *candidate-origin* attribution (accurate provenance,
  not a domain example).
- No change to the autonomy-layer copy (Art.11.2; regenerated by `upgrade`).

## Guardrails

- **edit-write-guard**: package-source knowledge, not a constitution file → not
  blocked; governed here.
- **boundary-guard**: edits in-boundary (cwd = this repo).
- Art.11.4 isolation: file already shipped via package-data — wheel re-checked.

## Phases

### Phase 1: Genericize all trade-domain examples + validate

- Deliverables:
  - `knowledge-html-profile.md`: items 1–7 above; profile → v1.1.1; `updated`
    bumped.
  - `pyproject.toml` + `__init__.py` version bump.
- Validation:
  - **grep gate → 0**: `option_selector|strangle|s50|signal_reader|artifacts/trade|
    期权|tier_breakdown|trade 2026|rules（as owner of an example）` absent (the
    v1.1.0 candidate-origin "trade-agent" line in §9 is the one allowed match).
  - `audit_html_profile.py` exit 0 (unchanged); full `tools/test_*.py` green.
  - `governance-core upgrade --project-root .` + `doctor` exit 0.
  - Wheel isolation: top-level only `governance_core*`, no `maintainer/` leak.
- Exit criteria: grep gate clean, tests green, doctor exit 0, wheel clean.

## Approval Criteria

- Every replaced example reads as governance-core's own domain; the syntax /
  contract lessons they illustrate are byte-for-byte preserved.
- Diff touches only example wording + version metadata — no rule/contract lines.
- The one intentional trade reference (v1.1.0 candidate-origin attribution in §9)
  remains.

## Validation Plan

- `grep -nE "<trade-token-set>" knowledge-html-profile.md` → only the §9
  candidate-origin line.
- `python tools/audit_html_profile.py`; full suite from repo-root `tools/`.
- `governance-core upgrade --project-root .` → `doctor`; `python -m build --wheel`
  + wheel top-level / leak assertion.

## Rollback / Recovery

- Single-file (+version) change; `git revert` restores the prior examples. No
  state, no contract dependency (examples are illustrative only).

## Risks

- **Miss a token** (low, low impact): incomplete de-trade. Mitigation: grep gate
  on the full token set in the validation plan.
- **gc autogen example is contrived** (low): the replacement governance-audit
  table must be plausible. Mitigation: model it on real gc audit output shape
  (checks × targets × pass/warn).

## State Log

- 2026-05-30: draft created by core agent (P-0086)
- 2026-05-30: draft → pending (submit de-trade residue cleanup)
- 2026-05-30: pending → approved (user approval signal: 也一并处理 (directive to handle the de-trade gap too))
- 2026-05-30: approved → implemented
