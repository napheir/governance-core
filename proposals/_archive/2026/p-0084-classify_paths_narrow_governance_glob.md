---
id: P-0084
agent: core
status: implemented
created: 2026-05-29
approved_at: 2026-05-29
implemented_in: 4ea8842
implemented_at: 2026-05-29
owner: core
---

# Proposal P-0084: Narrow classify-paths governance glob to exclude transient candidate-outbox (promote #19)

## Trigger

Hub-side curation of common-layer candidate **GitHub issue #19** (`mechanism`,
from `trade-agent`) via `/curate-candidate`. The candidate is a bug fix to the
classify fast-path config `proposal-classify-paths.json` (promoted in P-0080).

Proposal governance applies: the file is a **harness/routing gating config**
that drives the `proposal-classify-fast` PreToolUse hook and `classify --quick`
for every consumer — editing it changes default-deny gate behavior shipped via
`upgrade` (skill: "改自动化策略 / 改 generated state" → 必判 PROPOSAL_REQUIRED;
Art.11 package-source change).

**Bug (verified live with the real matcher, see Validation):** the `governance`
category catch-all glob `.governance/**/*` also matches the **transient,
gitignored** `.governance/candidate-outbox/` staging area (rooted at
`collect.py::OUTBOX_REL = ".governance/candidate-outbox"`). So every
candidate-envelope Edit/Write during `/submit-candidate` or `/upgrade`
drift-uplink yields a **false-positive `PROPOSAL_REQUIRED`** and the PreToolUse
hook hard-blocks it, forcing a spurious `classify --quick`. governance-core
itself is affected (self-hosting consumer) — strong dogfood signal.

## Scope

Edit **package source only** (`governance_core/tools/proposal-classify-paths.json`),
bump `version` 1.0.0 → 1.1.0:

- Replace the `governance` category catch-all `.governance/**/*` with the
  genuinely authoritative subpaths:
  `.governance/clauses/**/*` + `.governance/core_keywords.json` +
  `.governance/config.json`.
- This **excludes** the transient `.governance/candidate-outbox/` and the
  pure install-time derivative `.governance/installed_files.json`, while keeping
  full coverage of real governance sources.
- Add a `_refinement` doc note + per-glob `_why` clarification (provenance:
  promoted from #19 via P-0084).
- The matcher (`_classify_match.py`) has **no negation operator**, hence explicit
  enumeration rather than an exclude rule.

## Non-Goals

- No change to `_classify_match.py` (no negation operator added).
- No change to other categories (harness / routing / infra / settings).
- No change to the autonomy-layer copy `tools/proposal-classify-paths.json`
  (Art.11.2) — it is regenerated by `upgrade` from the package source.
- #18 (knowledge-html §2.5) is a separate proposal.

## Guardrails

- **edit-write-guard / proposal-classify-fast**: editing the **package-source**
  path `governance_core/tools/proposal-classify-paths.json` is NOT in the
  classify `routing` glob (that glob targets the autonomy-layer
  `tools/proposal-classify-paths.json`), so no self-block; the change is still
  governed here by this proposal.
- **boundary-guard**: all edits in-boundary (cwd = this repo).
- Art.11.4 package isolation: file already under `governance_core/tools/`,
  shipped via existing package-data glob — wheel-isolation re-checked at validation.

## Phases

### Phase 1: Apply refinement to package source + validate

- Deliverables:
  - `governance_core/tools/proposal-classify-paths.json`: governance globs
    narrowed, `version` → 1.1.0, `_refinement` note added.
  - `pyproject.toml` version bump (ships to consumers via `upgrade`).
- Validation:
  - Matcher unit check: `candidate-outbox/**` + `installed_files.json` →
    MISS; `clauses/**` + `core_keywords.json` + `config.json` + `CLAUDE.md`
    → HIT (both before/after table).
  - `tools/test_proposal_classify*.py` green (add a regression asserting
    candidate-outbox is NOT flagged).
  - `governance-core upgrade --project-root .` + `doctor` exit 0.
  - Wheel isolation (`python -m build --wheel`): top-level only
    `governance_core*`, file present, `maintainer/` absent.
- Exit criteria: tests green, doctor exit 0, wheel clean, JSON valid.

## Approval Criteria

- Before/after matcher table shows candidate-outbox + installed_files flip
  True→False while all authoritative governance paths remain True.
- Diff against current package source is exactly the governance-category glob
  narrowing + version/doc metadata (no collateral category edits).

## Validation Plan

- `python tools/_classify_match.py`-driven before/after table (already run
  during curation; re-run as a test).
- `python tools/test_proposal_classify_paths.py` + `test_proposal_classify.py`
  + `test_proposal_classify_fast_hook.py`.
- `python -c "import json; json.load(open('governance_core/tools/proposal-classify-paths.json'))"`.
- `governance-core upgrade --project-root .` → `governance-core doctor`.
- `python -m build --wheel` + wheel top-level assertion.

## Rollback / Recovery

- Single-file + version-bump change; `git revert` the implementation commit
  restores the v1.0.0 broad glob. No data migration, no state. `upgrade`
  re-derives the autonomy-layer copy from whichever package source is current.

## Risks

- **Under-coverage** (low/med impact): narrowing could miss a governance source
  that should require a proposal. Mitigation: enumerated set
  (clauses + core_keywords + config + CLAUDE.md + constitution + contracts +
  agent_rules) covers all authored governance; only the transient outbox and
  the install derivative are dropped — both correctly non-governance.
- **fnmatch glob semantics** (low): `**/` matches zero-or-more dirs; verified
  `.governance/clauses/**/*` still hits nested clause files.

## State Log

- 2026-05-29: draft created by core agent (P-0084)
- 2026-05-29: draft → pending (submit #19 classify-paths narrowing for review)
- 2026-05-29: pending → approved (user approval signal: 全部批准)
- 2026-05-29: approved → implemented
