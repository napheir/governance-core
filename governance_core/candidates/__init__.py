"""governance-core candidate pipeline (P-0065).

A candidate is a proposed common-layer capability -- a skill, a hook, or a
multi-file mechanism -- that a consuming project offers back to
governance-core for curation. The directory-shaped envelope format, its
builder, and its validator live in `envelope`.
"""
